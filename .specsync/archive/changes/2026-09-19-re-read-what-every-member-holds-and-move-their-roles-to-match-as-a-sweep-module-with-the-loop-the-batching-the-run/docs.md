---
change: re-read-what-every-member-holds-and-move-their-roles-to-match-as-a-sweep-module-with-the-loop-the-batching-the-run
artifact: docs
---

# Documentation

`README.md` gains a `Sweep` row, and its "no role sweep" bullet becomes "the
role sweep is written and nothing runs it", which is the true statement.

`docs/WHAT-IT-TALKS-TO.md` gains a `Sweep` row saying the thing a reader of a
disclosure document wants to know: it opens no connection of its own, it reads
the chain **through** `Chain` so a sweep is bounded by the same brakes as
everything else, and nothing links it.

Neither document claims the sweep runs, because it does not.