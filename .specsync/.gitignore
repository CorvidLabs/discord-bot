# spec-sync, generated by `specsync init`
# Committed: config.toml, agent-artifacts.json, changes/, archive/
# Ignored: backups, local config, hash cache (regenerated on each run)

backup-3x/
config.local.toml
hashes.json
change.lock
change-transaction.json
